# Build outputs
bin/
obj/

# Visual Studio
.vs/
*.user
*.suo
*.userosscache
*.userprefs

# Rider
.idea/

# NuGet
packages/
*.nupkg
project.lock.json
project.fragment.lock.json
artifacts/

# Tessdata (large binary, download at build time)
src/D4LootFilter/tessdata/
**/tessdata/*.traineddata

# Superpowers tooling state
.superpowers/

# OS
.DS_Store
Thumbs.db

# androguard scratch session state
androguard.db
androguard.db-shm
androguard.db-wal

# HayDay shared_prefs dumps contain live account tokens — never commit
docs/hayday/SEND ME 1 COIN!/
docs/hayday/SEND ME 1 COIN!.zip
docs/hayday/**/storage*.xml

# Scratch dir and large media captures (not source)
tmp/
/third_september.gif
/third_september_540.gif

# Grim Dawn: извлечённые данные игры (пересобираются пайплайном, gd.sqlite ~1 ГБ)
data/grim-dawn/

# Упакованные копии того, что уже лежит в репозитории исходниками
*.zip
*.7z

# Бинарные вложения входящих материалов (исходники — .md/.html рядом)
docs/inbox/*.docx
docs/inbox/*.png
