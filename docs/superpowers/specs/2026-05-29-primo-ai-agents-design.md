# Primo AI Agents — Архитектурная канва и техническое предложение

**Дата:** 2026-05-29  
**Автор:** Александр Шубин  
**Статус:** Черновик для обсуждения с техдиром  
**Цель документа:** Зафиксировать архитектуру, ключевые развилки и варианты MVP для продуктов "Сценарные ИИ агенты" и "Автономные ИИ агенты". Документ предназначен для принятия решений по архитектуре, стеку, границам продукта и приоритизации MVP.

---

## 1. Контекст и формулировка задачи

### 1.1 Что происходит

Primo AI Server — on-premise платформа для интеллектуальной автоматизации. Сегодня в продукте работают два зрелых блока: Умный OCR (обработка документов) и AI Text (обработка текста). Оба продаются.

Следующий шаг — ИИ агенты двух типов:

- **Сценарные ИИ агенты** (цель Ц1-2 в стратегическом документе) — набор шагов определён заранее, LLM работает внутри шагов: понимает запрос, отвечает по корпоративным знаниям, выполняет действие. Предсказуемы и управляемы. Статус: "начинаем продавать".
- **Автономные (рассуждающие) ИИ агенты** (цель Ц1-3) — сами прокладывают путь решения. Выбирают шаги, вызывают инструменты, действуют. Статус: архитектура, план на Q4 2026.

### 1.2 Решение руководства

ИИ агентов выносят как отдельные продукты в отдельную команду разработки с последующей интеграцией в общий скоуп продуктов Primo RPA.

### 1.3 Задача команды

Спроектировать и построить платформу для обоих типов агентов. Начать с MVP за 3-4 недели, запустить пилоты и пресейлы в кратчайшие сроки.

### 1.4 Команда

| Роль | Кол-во | Стек |
|------|--------|------|
| Tech Lead | 1 | Python, Go |
| Backend | 6 | Python, Go (гибкий стек) |
| Frontend | 1 | React/TypeScript |

### 1.5 Привязка к стратегическим целям

| Цель | Что проверяем к декабрю 2026 |
|------|------------------------------|
| Ц1-2 · Диалоговые агенты | Хотя бы один платный агент в проде месяц без откатов |
| Ц1-3 · Рассуждающие агенты | Оплаченный кейс + надёжность на реальном кейсе |
| Ц3 · Контур оркестрации | Подтверждён спрос + демо-пилот на реальном процессе |

---

## 2. Общая архитектура платформы

Ключевая идея: оба типа агентов — это разные **стратегии исполнения** поверх единой платформы. Платформа предоставляет общую инфраструктуру, а тип агента определяет только логику выбора следующего шага.

### 2.1 Схема компонентов

```
┌─────────────────────────────────────────────────────┐
│                   Chat UI (React)                   │
├─────────────────────────────────────────────────────┤
│                   API Gateway (Go)                  │
│            auth · rate-limit · routing              │
├──────────────┬──────────────────────────────────────┤
│              │       Agent Runtime (Python)         │
│  Agent       │  ┌────────────┐  ┌────────────────┐  │
│  Registry    │  │ Scenario   │  │  Autonomous    │  │
│  & Config    │  │ Executor   │  │  Executor      │  │
│              │  │ (граф)     │  │  (ReAct loop)  │  │
│              │  └─────┬──────┘  └───────┬────────┘  │
│              │        └────────┬────────┘           │
│              │         ┌──────┴───────┐            │
│              │         │ Tool Router  │            │
├──────────────┘         └──────┬───────┘            │
├───────────────────────────────┼─────────────────────┤
│           Platform Services   │                     │
│  ┌───────────┐ ┌───────────┐ │  ┌───────────────┐  │
│  │ LLM       │ │ State &   │ │  │ Governance    │  │
│  │ Provider  │ │ Context   │ │  │ RBAC · Audit  │  │
│  │ Abstraction│ │ Store    │ │  │ Guardrails    │  │
│  └───────────┘ └───────────┘ │  └───────────────┘  │
├──────────────────────────────┼─────────────────────┤
│              Tool Layer      │                      │
│  ┌─────┐ ┌───────┐ ┌──────┐ │ ┌─────┐ ┌────────┐  │
│  │ 1С  │ │ Email │ │ RAG  │ │ │ RPA │ │ Custom │  │
│  │     │ │       │ │ Docs │ │ │Robot│ │ Tools  │  │
│  └─────┘ └───────┘ └──────┘   └─────┘ └────────┘  │
└─────────────────────────────────────────────────────┘
```

### 2.2 Компоненты и стек

| Компонент | Язык | Назначение |
|-----------|------|-----------|
| **API Gateway** | Go | Единая точка входа: аутентификация, роутинг, rate limiting |
| **Agent Runtime** | Python | Ядро исполнения агентов. Два executor'а с общим интерфейсом |
| **Tool Router** | Python | Единый реестр инструментов. Агент вызывает tool по имени, роутер маршрутизирует |
| **LLM Provider** | Python | Абстракция над LLM: AI Server, Ollama, vLLM, OpenAI (dev/test). Единый интерфейс |
| **State & Context Store** | Go/Python + PostgreSQL | Состояние сессии, история диалога, контекст между шагами |
| **Governance** | Go | RBAC, аудит-лог, guardrails |
| **Chat UI** | React/TypeScript | Чат с агентом, трейс рассуждений, журнал шагов |
| **Tool Layer** | Python | Конкретные интеграции: 1С, почта, RAG, RPA через MCP |

**Принцип разделения Go/Python:** Go — инфраструктура (API, auth, governance, scheduling). Python — AI-логика (runtime, tools, LLM). Чёткая граница, минимум пересечений.

---

## 3. Agent Runtime — движок исполнения

### 3.1 Общий интерфейс

```
AgentExecutor:
  input:  UserMessage + Context + ToolRegistry
  output: stream of AgentEvent (thought, tool_call, tool_result, answer, approval_request)
```

Все события — единый поток `AgentEvent`. UI, логирование и governance работают одинаково для обоих типов агентов.

### 3.2 Scenario Executor

```
                    ┌──────────────┐
                    │  Step Graph   │
                    │  (YAML/JSON)  │
                    └──────┬───────┘
                           │
              ┌────────────▼────────────┐
              │    Step 1: classify     │
              │    LLM: "определи тип  │
              │    документа"           │
              └────────────┬────────────┘
                      ┌────┴─────┐
                 invoice?     contract?
                      │          │
              ┌───────▼──┐  ┌───▼────────┐
              │ Step 2a:  │  │ Step 2b:   │
              │ extract   │  │ summarize  │
              │ requisites│  │ terms      │
              └───────┬───┘  └───┬────────┘
                      └────┬─────┘
              ┌────────────▼────────────┐
              │  Step 3: action (1С)    │
              │  human approval: true   │
              └─────────────────────────┘
```

- Граф шагов определяется в YAML/JSON (позже — через визуальный конструктор)
- Каждый шаг: prompt для LLM + допустимые tools + условия перехода
- LLM работает **внутри** шага, но не выбирает следующий шаг
- Детерминированный маршрут, предсказуемое поведение

### 3.3 Autonomous Executor

```
         ┌──────────────────────────┐
         │   System Prompt +        │
         │   Task Description +     │
         │   Available Tools        │
         └────────────┬─────────────┘
                      │
              ┌───────▼───────┐
              │   THINK       │◄── LLM рассуждает о следующем шаге
              └───────┬───────┘
                      │
              ┌───────▼───────┐
              │   ACT         │◄── вызов tool
              └───────┬───────┘
                      │
              ┌───────▼───────┐
              │   OBSERVE     │◄── оценка результата
              └───────┬───────┘
                      │
                 ┌────┴────┐
            задача     нужен ещё
            решена?    шаг?
                 │          │
          ┌──────▼──┐  ┌────▼─────┐
          │ ANSWER  │  │ THINK    │──► повтор цикла
          └─────────┘  └──────────┘
```

- ReAct-цикл: Think → Act → Observe → (repeat or Answer)
- LLM сам выбирает, какой tool вызвать и когда остановиться
- Human-in-the-loop: настраиваемый — на каждом действии, только на критических, или только на финальном результате
- Ограничения: макс. число шагов, запрещённые действия, таймаут

### 3.4 Общие компоненты (реализуются один раз)

| Компонент | Описание |
|-----------|----------|
| **ToolRegistry** | Реестр доступных инструментов с описаниями для LLM |
| **ContextManager** | Управление контекстом: история, промежуточные результаты, файлы |
| **EventStream** | Единый поток событий (thought, tool_call, result, approval) |
| **HumanApproval** | Механизм запроса подтверждения у оператора |
| **GuardrailsEngine** | Проверка действий агента перед исполнением |
| **SessionStore** | Персистентное состояние сессии (PostgreSQL) |

---

## 4. Tool Layer и интеграции

### 4.1 Протокол инструмента

```
Tool:
  name: str                  # уникальный идентификатор
  description: str           # описание для LLM
  parameters: JSONSchema     # входные параметры
  requires_approval: bool    # нужно ли подтверждение оператора
  execute(params) → Result   # синхронный или async вызов
```

Каждый tool — Python-класс с единым интерфейсом. Добавление нового инструмента — один файл, регистрация в реестре.

### 4.2 Интеграции

| Tool | Что делает | Фаза |
|------|-----------|------|
| **1С (REST/OData)** | Чтение справочников, документов, контрагентов. Создание/проведение документов. | MVP |
| **Email (IMAP/SMTP)** | Чтение входящих, парсинг вложений, отправка ответов. | MVP |
| **RAG / Document Search** | Поиск по загруженным документам. Векторный поиск + цитирование со ссылкой на источник. | MVP |
| **AI Server OCR** | Распознавание документов и сканов через API AI Server. | MVP |
| **AI Server NLP** | Классификация, извлечение данных из текста через API AI Server. | MVP |
| **RPA Robot (MCP)** | Вызов RPA-робота для действий в системах без API. | Post-MVP |
| **HTTP / REST** | Универсальный tool для вызова произвольных API. | Post-MVP |
| **File System** | Чтение/запись файлов, работа с сетевыми папками. | Post-MVP |

### 4.3 RAG-подсистема

```
Документы ──► Chunking ──► Embedding ──► Vector Store (pgvector)
                                            │
Запрос агента ──► Embedding ──► Search ─────┘
                                    │
                              Top-K chunks ──► LLM ──► Ответ со ссылкой на источник
```

- **Vector Store:** pgvector (PostgreSQL, уже в стеке)
- **Embedding:** локальная модель (e5, BGE) через LLM Provider абстракцию
- **Chunking:** по документам, с сохранением метаданных (источник, страница, дата)
- **На выходе:** ответ + цитата + ссылка на источник (требование enterprise-заказчика)

### 4.4 Интеграция с 1С

```
Agent ──► Tool Router ──► 1C Tool ──► 1C REST API (OData)
                                          │
                                    ┌─────┴──────┐
                                    │ Справочники │
                                    │ Документы   │
                                    │ Регистры    │
                                    └─────────────┘
```

- 1С 8.x публикует OData-интерфейс из коробки
- Tool предоставляет агенту высокоуровневые операции: `find_partner`, `get_invoice`, `create_document`, `post_document`
- Агент не знает про OData — вызывает tool по имени с параметрами, tool транслирует в запрос

---

## 5. LLM Provider — абстракция над моделями

### 5.1 Схема

```
┌─────────────────────────────────────┐
│          Agent Runtime              │
│   llm.chat(messages, tools)         │
│   llm.embed(text)                   │
└──────────────┬──────────────────────┘
               │
       ┌───────▼────────┐
       │  LLM Provider  │
       │  Interface     │
       └───────┬────────┘
               │
    ┌──────────┼──────────┬──────────────┐
    │          │          │              │
┌───▼───┐ ┌───▼───┐ ┌────▼────┐  ┌─────▼─────┐
│  AI   │ │ Ollama│ │  vLLM   │  │  OpenAI   │
│Server │ │       │ │         │  │  (dev)    │
└───────┘ └───────┘ └─────────┘  └───────────┘
  prod      prod      prod        dev/test
```

### 5.2 Интерфейс

| Метод | Назначение |
|-------|-----------|
| `chat(messages, tools?, stream?)` | Генерация: рассуждение, ответ, вызов tools. Поддержка tool calling и streaming. |
| `embed(text) → vector` | Векторное представление для RAG. |

### 5.3 Конфигурация

```yaml
llm:
  provider: ai_server        # ai_server | ollama | vllm | openai
  endpoint: http://ai-server:8080/api
  model: qwen2.5-72b
  
  embedding:
    provider: ollama
    model: bge-m3
```

Переключение провайдера — одна строка в конфиге, код агента не меняется.

### 5.4 Ограничения

- Не все провайдеры поддерживают tool calling нативно. Fallback: prompt-based tool calling (парсинг JSON из ответа LLM).
- Streaming: нормализуется абстракцией для единообразного UX.

---

## 6. Governance — контроль и безопасность

Сквозной слой, работает одинаково для обоих типов агентов. Без этого enterprise-заказчик не пустит агента в контур.

### 6.1 RBAC

```
User ──► Role ──► Permissions
                    │
         ┌─────────┼──────────┐
         │         │          │
    agents:use  tools:1c   agents:admin
                  :read     (создание/настройка)
```

- Роли и пермишены, интеграция с AD/LDAP/Keycloak (SSO)
- Разграничение: кто пользуется агентом, кто настраивает, какие tools доступны какой роли
- MVP: базовая модель (admin / operator), без SSO

### 6.2 Audit Log

Каждое действие агента — запись в лог:

```json
{
  "timestamp": "...",
  "session_id": "...",
  "agent_id": "...",
  "user_id": "...",
  "event": "tool_call",
  "tool": "1c.post_document",
  "params": { "doc_id": "..." },
  "reasoning": "Счёт сверен, расхождений нет, провожу документ",
  "result": "success",
  "approved_by": "operator@company.ru"
}
```

- Полная трассировка: от запроса пользователя до финального действия
- Для автономных агентов — цепочка рассуждений (reasoning trace)
- Хранение: PostgreSQL, retention policy настраивается

### 6.3 Guardrails

```
Agent ──► action ──► Guardrails Check ──► разрешено? ──► Tool Execution
                                              │
                                         запрещено? ──► блокировка + лог
```

- Чёрный список действий (например: "нельзя удалять документы в 1С")
- Лимиты: макс. число шагов, макс. сумма операции, таймаут сессии
- Content filtering: проверка входа/выхода LLM на чувствительные данные (PII)
- MVP: базовые лимиты (макс. шагов, таймаут). Полные guardrails — следующая фаза.

---

## 7. Потоки данных

### 7.1 Основной поток (автономный агент)

```
Пользователь              Chat UI           API Gateway        Agent Runtime
     │                      │                   │                   │
     │ "Разбери этот счёт"  │                   │                   │
     ├─────────────────────►│                   │                   │
     │                      ├──auth + route────►│                   │
     │                      │                   ├──create session──►│
     │                      │                   │                   │
     │                      │◄──────── stream AgentEvent ──────────┤
     │                      │                   │                   │
     │ [thought] "Нужно     │                   │             ┌─────┤
     │◄─найти счёт в 1С"───┤                   │             │THINK│
     │                      │                   │             └─────┤
     │                      │                   │                   │
     │ [tool_call]          │                   │             ┌─────┤
     │◄─1c.find_invoice────┤                   │             │ ACT │
     │                      │                   │             └──┬──┤
     │                      │                   │            ┌───▼──┐
     │                      │                   │            │1С   │
     │                      │                   │            │Tool  │
     │                      │                   │            └───┬──┘
     │ [thought] "Сумма не  │                   │             ┌──▼──┤
     │◄─совпадает"─────────┤                   │             │OBSRV│
     │                      │                   │             └─────┤
     │                      │                   │               ... │
     │ [approval_request]   │                   │             ┌─────┤
     │◄─"Провести с корр.?"┤                   │             │APPRV│
     │                      │                   │             └─────┤
     ├──"Да"───────────────►├──────────────────►├──────────────────►│
     │                      │                   │                   │
     │ [answer] "Проведён"  │                   │             ┌─────┤
     │◄────────────────────┤                   │             │ANSWR│
     │                      │                   │             └─────┤
```

Поток для сценарного агента — идентичен, но вместо цикла THINK/ACT/OBSERVE идёт последовательность заданных шагов. EventStream и UI — одинаковые.

### 7.2 Поток RAG

```
Agent: "Найди условия по договору с ООО Ромашка"
  │
  ▼
RAG Tool
  ├──► Embed запрос ──► Vector Store ──► Top-5 chunks
  │                                         │
  ├──► Prompt: контекст + вопрос ◄──────────┘
  ├──► LLM ──► ответ + ссылки на источники
  ▼
Agent: "Отсрочка 30 дней, скидка 5% при объёме >1М₽
        [Источник: Договор_123.pdf, стр.3]"
```

---

## 8. Deployment и инфраструктура

### 8.1 Модель поставки

Docker Compose для MVP и пилотов. Kubernetes (Helm) для production у крупного заказчика. Архитектура одна, меняется оркестрация.

### 8.2 Docker Compose (MVP / пилот)

```
┌─────────────────────────────────────────┐
│  Host (Linux, on-premise)               │
│                                         │
│  ┌──────────┐  ┌──────────────────────┐ │
│  │ nginx    │  │ agent-runtime        │ │
│  │ (proxy)  ├─►│ Python, uvicorn      │ │
│  └──────────┘  └──────────┬───────────┘ │
│                           │             │
│  ┌──────────┐  ┌──────────▼───────────┐ │
│  │ chat-ui  │  │ api-gateway          │ │
│  │ React    │  │ Go                   │ │
│  │ (static) │  └──────────────────────┘ │
│  └──────────┘                           │
│  ┌──────────┐  ┌──────────────────────┐ │
│  │PostgreSQL│  │ Redis                │ │
│  │+ pgvector│  │ (sessions, cache)    │ │
│  └──────────┘  └──────────────────────┘ │
└─────────────────────────────────────────┘

Внешние зависимости (у заказчика):
  ├── AI Server (LLM inference)
  ├── 1С (OData API)
  └── LDAP/AD (post-MVP)
```

### 8.3 Минимальные требования

- Linux-сервер (или VM), 4 CPU, 16 GB RAM
- PostgreSQL 15+ с pgvector
- Сетевой доступ до AI Server и 1С
- Docker + Docker Compose
- **GPU не требуется** — агенты не крутят LLM, только вызывают. GPU нужен только AI Server.

---

## 9. Два варианта MVP

Общая архитектура, стек и подходы не меняются. Вариант MVP определяет только, какой тип агента строим первым.

### 9.1 Развилка: фреймворк агентов

| Вариант | Плюсы | Минусы |
|---------|-------|--------|
| **Кастомный runtime** | Полный контроль, нет чужих абстракций | Всё пишем сами, дольше до MVP |
| **LangGraph** | Зрелый, поддерживает графы и ReAct, большое комьюнити | Тяжёлая зависимость, vendor lock на LangChain |
| **smolagents (HuggingFace)** | Лёгкий, минимум абстракций, совпадает с прототипом PO | Молодой проект, меньше production-кейсов |
| **CrewAI** | Мультиагентность из коробки | Избыточен для старта |

**Рекомендация:** smolagents или LangGraph для MVP, решение о production-фреймворке — после MVP на основе опыта.

### 9.2 Вариант A: MVP на сценарных агентах

**Привязка к стратегии:** цель Ц1-2 (диалоговые агенты). Все способности — фаза Run.

**Скоуп (3-4 недели):**

| Неделя | Что делаем | Кто |
|--------|-----------|-----|
| 1 | LLM Provider абстракция, Tool интерфейс, скелет API Gateway | 3 backend |
| 1 | Scaffold Chat UI (чат + лог шагов) | 1 frontend |
| 1-2 | Scenario Executor: DSL графа (YAML), движок шагов, переходы | 2 backend |
| 2 | Tools: 1С (OData), Email (IMAP/SMTP) | 2 backend |
| 2-3 | RAG: chunking, pgvector, поиск, цитирование | 1 backend |
| 3 | Интеграция: сквозной сценарий "обработка счёта" | вся команда |
| 3-4 | Базовый audit log, human approval в UI | 1 backend + frontend |
| 4 | Стабилизация, демо-сценарий, документация | вся команда |

**Демо-сценарий:** "Обработка входящего счёта" — агент получает счёт, извлекает реквизиты (через OCR/AI Server), сверяет с данными в 1С, при расхождении запрашивает подтверждение, проводит документ.

**Результат:** работающий сценарный агент, чат-интерфейс, логирование.  
**Не входит:** визуальный конструктор (только YAML), RBAC/SSO, guardrails, автономный режим.

**Риск по срокам:** движок графов с DSL, переходами и ветвлениями — 2-3 недели на двоих. Если застрянем на DSL — MVP сдвигается. Это самый тяжёлый компонент варианта A, которого нет в варианте B.

### 9.3 Вариант B: MVP на автономных агентах

**Привязка к стратегии:** цель Ц1-3 (рассуждающие агенты). Движок — фаза Discovery. Инструменты и governance — Run.

**Скоуп (3-4 недели):**

| Неделя | Что делаем | Кто |
|--------|-----------|-----|
| 1 | LLM Provider абстракция, Tool интерфейс, скелет API Gateway | 3 backend |
| 1 | Scaffold Chat UI (чат + трейс рассуждений) | 1 frontend |
| 1-2 | Autonomous Executor: ReAct цикл, tool calling, stop condition | 2 backend |
| 2 | Tools: 1С (OData), Email (IMAP/SMTP) | 2 backend |
| 2-3 | RAG: chunking, pgvector, поиск, цитирование | 1 backend |
| 3 | Human-in-the-loop: approval UI, уровни контроля | 1 backend + frontend |
| 3 | Базовый audit log с reasoning trace | 1 backend |
| 3-4 | Демо-сценарий "разбор исключения по счёту", стабилизация | вся команда |
| 4 | Guardrails: макс. шаги, таймаут, запрещённые действия | 1 backend |

**Демо-сценарий:** "Разбор исключения по счёту" — агент получает застрявший счёт, сам решает куда смотреть: лезет в 1С за историей контрагента, проверяет условия договора, сравнивает позиции, формулирует причину расхождения, предлагает решение. Оператор видит ход рассуждения и подтверждает.

**Результат:** рассуждающий агент с видимым ходом мысли, чат-интерфейс, логирование, human-in-the-loop.  
**Не входит:** сценарный режим, визуальный конструктор, RBAC/SSO, сложные guardrails.

**Риск по срокам:** ReAct-цикл проще движка графов — ядро за 1-1.5 недели на двоих. Основной риск — качество рассуждений LLM. Митигация: на демо использовать мощную модель (OpenAI), для production проверять на локальных моделях отдельно.

### 9.4 Сравнение

| Критерий | Вариант A (сценарный) | Вариант B (автономный) |
|----------|----------------------|------------------------|
| **Сложность MVP** | Выше (DSL графов) | Ниже (ReAct-цикл) |
| **Риск по срокам** | Средний (движок графов) | Низкий (код), средний (качество LLM) |
| **Wow-фактор на демо** | Умеренный | Высокий |
| **Предсказуемость демо** | Высокая | Средняя (зависит от LLM) |
| **Доверие enterprise** | Высокое | Среднее (нужен human-in-the-loop) |
| **Дифференциация на рынке** | Слабая (workflow + AI = commoditized) | Сильная (рассуждающий агент) |
| **Путь ко второму типу** | B после A — сложнее | A после B — проще (ReAct + фиксированный план = scenario) |
| **Цель по стратегии** | Ц1-2 (Run, начинаем продавать) | Ц1-3 (Discovery, проверка гипотезы) |

**Рекомендация:** Вариант B — быстрее до MVP, сильнее на демо, проще путь к добавлению сценарного режима потом. Решение за командой.

---

## 10. Фазы развития до конца 2026

```
    Июнь          Июль-Авг       Сент-Окт        Нояб-Дек
┌──────────┐  ┌───────────┐  ┌────────────┐  ┌─────────────┐
│  Фаза 0  │  │  Фаза 1   │  │  Фаза 2    │  │  Фаза 3     │
│  MVP      │  │  Пилот +  │  │ Production │  │  Масштаб    │
│  3-4 нед  │  │  2й тип   │  │  Grade     │  │             │
└──────────┘  └───────────┘  └────────────┘  └─────────────┘
```

### Фаза 0: MVP (июнь, 3-4 недели)

**Цель:** работающий демо-прототип для первых пилотов и пресейлов.

| Результат | Фаза по стратегии |
|-----------|-------------------|
| Один тип агента (A или B) | Discovery |
| LLM Provider абстракция (OpenAI для демо) | Run |
| Tools: 1С, Email, RAG | Run |
| Chat UI с трейсом | Run |
| Базовый audit log | Run |
| Human-in-the-loop | Run |
| Docker Compose деплой | Run |

**Проверяем:** можно ли показать заказчику ценность и получить интерес к пилоту.

### Фаза 1: Пилот + второй тип агента (июль-август, 6-8 недель)

**Цель:** первый пилот у заказчика + добавление второго типа агента.

| Результат | Фаза по стратегии |
|-----------|-------------------|
| Второй тип агента | Run |
| Проверка на локальных LLM (AI Server, Ollama) | Discovery (Ц1-3 надёжность) |
| RBAC: роли admin / operator / viewer | Run |
| SSO: интеграция с AD/LDAP | Run |
| Agent Builder v0: конфигурация через web-форму | Run |
| Мониторинг: дашборд работы агентов | Run |

**Проверяем:** держит ли агент надёжность на локальной модели? Платит ли заказчик?

### Фаза 2: Production Grade (сентябрь-октябрь, 6-8 недель)

**Цель:** продукт для прода у заказчика.

| Результат | Фаза по стратегии |
|-----------|-------------------|
| Полноценные Guardrails | Run |
| Audit log compliance-уровня | Run |
| Отказоустойчивость: retry, degradation, health checks | Run |
| Kubernetes-деплой (Helm chart) | Run |
| Agent Builder v1: визуальный конструктор (если подтверждён спрос) | Discovery → Run |
| RPA-интеграция через MCP | Run |
| Расширение tools: HTTP/REST, файловая система | Run |

**Проверяем:** может ли продукт работать в проде без нашего сопровождения.

### Фаза 3: Масштаб (ноябрь-декабрь)

**Цель:** масштабирование продаж, подготовка к оркестрации.

| Результат | Фаза по стратегии |
|-----------|-------------------|
| Мультиагентность: несколько агентов в одном процессе | Discovery (Ц3) |
| Контур оркестрации v0: агент + робот + человек | Discovery (Ц3) |
| Конструктор рассуждающего агента (если подтверждена реализуемость) | Discovery (Ц1-3) |
| Marketplace tools: механизм добавления кастомных инструментов | Run |
| Метрики: стоимость сессии, время решения, success rate | Run |

**Проверяем к декабрю 2026:**
- Ц1-2: есть ли платный агент в проде месяц без откатов?
- Ц1-3: есть ли оплаченный кейс рассуждающего агента?
- Ц3: подтверждён ли спрос на контур оркестрации?

---

## 11. Развилки для обсуждения

Решения, которые нужно принять до начала разработки.

| # | Развилка | Вариант А | Вариант Б | Рекомендация |
|---|----------|-----------|-----------|--------------|
| 1 | **MVP: сценарный или автономный** | Предсказуемо, ближе к продаже (Ц1-2) | Быстрее до MVP, сильнее на демо (Ц1-3) | B, решение за командой |
| 2 | **Фреймворк агентов** | Кастомный runtime — полный контроль | smolagents/LangGraph — быстрый старт | smolagents для MVP, production — по итогам |
| 3 | **Отношение к AI Server** | Модуль внутри AI Server — общий UI, auth, инфра | Standalone продукт — свой деплой, API-интеграция | Обсудить с техдиром |
| 4 | **Agent Builder** | Строим сами | Переиспользуем существующий из AI Server | Зависит от развилки #3 |
| 5 | **Vector Store** | pgvector (уже в стеке) | Qdrant (быстрее на больших объёмах) | pgvector на старте |
| 6 | **Протокол tools** | Свой протокол | MCP (стандарт, совместимость) | MCP |

---

## 12. Риски

| Риск | Вероятность | Влияние | Митигация |
|------|------------|---------|-----------|
| Качество рассуждений LLM на локальных моделях недостаточно | Средняя | Высокое — автономный агент не работает | OpenAI для демо, параллельная проверка локальных моделей |
| 3-4 недели на MVP — недостаточно | Средняя | Среднее — сдвиг пилотов | Жёсткий скоуп, cut features а не сроки |
| Интеграция с 1С сложнее ожидаемого (нестандартные конфигурации у заказчиков) | Средняя | Среднее | Начать с типовой конфигурации, кастомизация на пилоте |
| AI Server API не покрывает наши потребности по inference | Низкая | Высокое | Абстракция LLM Provider позволяет обойти через Ollama/vLLM |
| Конкуренция с внутренней командой AI Server за скоуп | Средняя | Высокое | Чёткие границы продуктов в этом документе |

---

## 13. Ближайшие шаги

### Неделя 0 (до начала разработки)

| Действие | Ответственный | Результат |
|----------|--------------|-----------|
| Обсуждение документа с техдиром | Tech Lead | Решение по развилкам #1-6 |
| Согласование MVP-скоупа с PO | Tech Lead | Подтверждённый скоуп и демо-сценарий |
| Выбор фреймворка: spike 1-2 дня (smolagents vs LangGraph) | 2 backend | Обоснованный выбор |

### Неделя 1 (начало разработки)

| Задача | Кто |
|--------|-----|
| Инициализация репозитория, CI/CD, линтеры, структура проекта | Tech Lead |
| LLM Provider абстракция: интерфейс + OpenAI-адаптер | 1 backend |
| Tool интерфейс + Tool Router + первый tool (1С read-only) | 2 backend |
| Agent Executor (выбранный тип): базовый цикл | 2 backend |
| API Gateway: скелет, health check, WebSocket для streaming | 1 backend |
| Chat UI: scaffold, чат, отображение событий | 1 frontend |

### Критерий готовности MVP

MVP считается готовым, когда можно провести демо сквозного сценария перед потенциальным заказчиком:

1. Пользователь пишет задачу в чат
2. Агент рассуждает / выполняет шаги (видно в UI)
3. Агент обращается к 1С и/или почте
4. Агент запрашивает подтверждение на критическое действие
5. Пользователь подтверждает
6. Агент выполняет действие и отвечает результатом
7. Весь процесс записан в audit log
